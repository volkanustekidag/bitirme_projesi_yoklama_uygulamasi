class Notification {
  final int postId;
  final String postTeacherId, postContent;
  final DateTime postDate;

  Notification(
      this.postId, this.postTeacherId, this.postContent, this.postDate);
}
